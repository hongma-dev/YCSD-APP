#Server Port
server:
  port: 8080

spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost/~/YCSD-APP
      username: hongma
      password:
      driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        show_sql : true
        format_sql : true


logging:
  level:
    org.hibernate.SQL : debug
