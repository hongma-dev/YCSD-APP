#Server Port
server:
  port: 8080

spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://ec2-3-139-91-9.us-east-2.compute.amazonaws.com:3306/YCSD
      username: root
      password: hongma123
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        show_sql : true
        format_sql : true


logging:
  level:
    org.hibernate.SQL : debug
